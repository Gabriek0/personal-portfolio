## Personal Portfolio

## Local Decap CMS

Run these commands in separate terminals:

```bash
pnpm dev
pnpm cms:local
```

Then open `http://localhost:3000/admin`. The Decap proxy runs on port `8081` and writes edits to the local Git repository. Review the resulting JSON changes before committing them.

### 🚧 Under development 🚧
